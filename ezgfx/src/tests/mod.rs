use crate::*;

// no test, just making sure it doesn't err
fn test_pipeline_api()
{
    let renderer = Renderer::from_window(todo!());

    // renderer
    //     .pipeline()
    //         .set(0)
    //             .binding(0, ShaderKind::Vertex, res)


}